# Design.md — Codex Taskboard

## 产品角色

这是 Codex 的全局任务工作层，不是某个对话里的附件，也不是传统团队项目管理 SaaS。任务先独立存在于看板，再按需交给一个或多个 Codex 对话执行。用户需要在一个屏幕里看清：什么还没开始、Codex 正在做什么、什么等待人工确认，以及每个任务关联了哪些执行上下文。

## 受众与场景

- 受众：同时运行多个 Codex 项目和长任务的开发者。
- 主场景：从 Codex 全局侧栏进入持续存在的看板，在任务和 Codex 对话间往返。
- 单一工作：把任务从需求写入一直推进到人工验收，并保留可追溯上下文。

## 信息层级

1. Codex 全局侧栏入口、项目与跨项目视图。
2. `待办 → 处理中 → 待确认` 的主工作流。
3. 任务标题、优先级、标签、执行进度、评论与关联对话。
4. 任务详情中的描述、附件、关系、分支 / worktree 与活动记录。
5. 已阻塞、已完成、已取消和已归档等非主流程状态。

## 交互模型

- 看板是独立于对话的主视图；列表、甘特图和 Dashboard 是同一数据的不同观察面。
- 新建任务从当前项目和当前列继承上下文。
- 卡片可拖动或通过状态菜单推进；写入后由实时事件同步所有打开页面。
- 点击卡片进入独立任务详情；手动模式下，`Open in new conversation` 只在显式触发时创建 Codex composer / 线程关联。开启自动认领后，后台策略会为已授权的 Todo 创建或复用执行会话；两种路径都不会自动接受或完成任务。
- 关闭或切换对话不会删除任务；任务状态和活动历史以 Taskboard SQLite 为准。
- `done` 是人工验收后的终态，执行完成先进入 `in_review`。

## 组件语法

- Codex 宿主侧栏：固定 `Taskboard` 入口，与 New chat、Sites、Plugins 等全局功能同级。
- Taskboard 内部导航：低对比中性灰，显示项目和主入口。
- 顶栏：轻量分段导航，视图切换不抢占内容注意力。
- 看板列：状态色只出现在列标题、状态图标和进度线，不大面积铺色。
- 任务卡：白色工作票据，标题优先，元信息贴近底部；圆角克制，阴影仅用于层级。
- 详情面板：文档阅读区 + 属性区，Markdown、附件和活动流保持可扫读。
- 反馈：写入、错误、连接状态和冲突必须在动作附近可见。

## 视觉令牌

### 浅色

- Canvas `#FCFCFD`
- Sidebar `#F3F3F4`
- Surface `#FFFFFF`
- Ink `#1B1B1B`
- Muted ink `#5D5D5F`
- Hairline `#E1E1E1`
- Action blue `#317CFF`

### 状态色

- Backlog / Todo `#9E9EA1`
- In progress `#F0BF00`
- In review `#43BC58`
- Blocked `#F34E52`
- Done `#5E6AD2`

### 深色

- Canvas `#171719`
- Raised surface `#27272A`
- Ink `#EEEEEF`
- Hairline `rgba(255,255,255,.075)`

## 字体与密度

- UI：系统 UI 字体栈，中文优先 PingFang SC，保证与 Codex 宿主一致。
- 12px 为主要工作密度，标题和主动作使用 13–16px；不使用营销页式大标题。
- 8px 基础间距，卡片和列保持高信息密度。

## 响应式与可访问性

- 最小宽度 320px；窄屏允许看板横向浏览，详情保持可读。
- 所有主要动作可键盘聚焦，焦点环使用 Action blue。
- 深浅主题都保持文字、边界和状态可辨。
- 动画仅用于状态切换与反馈，并尊重 `prefers-reduced-motion`。

## 设计签名

“执行脉冲”是唯一强调：处理中卡片用细分段进度线表达 Codex 的真实运行节奏。其余界面保持安静，让工作状态本身成为视觉重点。

## QA

- 原尺寸检查 Codex 全局工作区、1440px 桌面 Web 与 390px 窄屏 Web。
- 确认侧栏 `Taskboard` 与对话列表同级，打开后主工作区不显示对话 composer。
- 从独立任务详情确认 `Open in new conversation` 是显式下游动作。
- 检查看板列滚动、卡片拖动、详情遮挡、菜单裁切、长标题换行和中文字体。
- 验证浅色、深色、键盘焦点和减少动态效果。
